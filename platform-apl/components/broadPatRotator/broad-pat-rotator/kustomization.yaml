# spec.components.broadPatRotator → this kustomize Component. Enable on EXACTLY ONE
# deployment (the broad PAT is account-wide). Template-managed. See
# docs/designs/credential-single-pane.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - network-policy.yaml
  # mTLS identity + serving-CA anchor for the OpenBao hop (wave -14).
  - client-tls-certificate.yaml
  - externalsecret.yaml
  - openbao-ca-bundle.yaml
  - cronjob.yaml

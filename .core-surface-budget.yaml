# Core-surface budget — Go logic lines in package main (tools/cmd/llz).
#
# WHY THIS EXISTS, and why it has no slack:  docs/adr/0014-core-surface-budget.md
# WHERE THE NUMBER IS GOING (~3,000):        docs/designs/internal-extensions.md
# WHAT A BREACH TELLS YOU:                   budget.CoreSurfaceRemedy, in
#                                            tools/internal/budget/remedy.go
#
# This file is the operational half of that: the number, and the rule for
# changing it. The argument lives in the ADR and is not repeated here.
#
# THE RULE. `budget` equals the measurement exactly — it is a high-water mark, not
# a ceiling with headroom (`exact: true` enforces both directions). So:
#
#   grew package main?    the gate fails until you raise this line in the SAME
#                         commit, and say in the message why the code belongs in
#                         package main rather than tools/internal/<pkg>.
#   shrank it?            the gate fails until you LOWER this line. Bank the
#                         paydown; slack left behind silently pre-approves the
#                         next change's growth.
#
# Raising it is expected, not forbidden. The signal is the trend of this line, not
# any single value.
#
# COUNTING (`llz ci core-surface --verbose` prints the per-file breakdown).
# `go-logic` counts non-blank lines of Go that do not START with `//` — the
# `script` counter's rule with Go's comment marker. Block comments are
# deliberately not tracked: this repo documents with `//`, and `/*` appears almost
# only inside glob and regex string literals, where a naive scanner would swallow
# the rest of the file.
#
# SCOPE. Non-test Go directly under tools/cmd/llz — package main, the one package
# that cannot be imported, cannot be unit-tested from outside, and has no boundary
# of its own. tools/internal/** is NOT counted: code that moved there has already
# done the thing this gate asks for. Tests are excluded outright, because they are
# what this repo wants more of.
#
# NO `remedy:` KEY, deliberately. The breach guidance lives only in
# budget.CoreSurfaceRemedy: when it lived in both places the YAML copy silently
# won, and an edit to the Go constant never reached an operator. The key stays supported
# for a gate that needs to override the default; this one does not.

categories:
  cmd-llz-package-main:
    kind: go-logic
    # 38,364 logic lines across 202 non-test files, 113 of them ci_*.go.
    #
    # THIS LINE HAS NOW MOVED IN BOTH DIRECTIONS, which is the only evidence that
    # matters about a ratchet:
    #
    #   41,803 / 215 files  2026-08-03. The first number the gate ever recorded.
    #   47,182 / 236 files  2026-08-05, on rebase. +5,379 in five weeks of `main`
    #                       with nothing extracted — the trend ADR 0014 predicted,
    #                       arriving on schedule and restated by hand because the
    #                       gate went red until someone did.
    #   46,716 / 234 files  the guard-budgets EXTRACTION. The budget engine, the
    #                       counters, the remedies and their tests are now
    #                       tools/internal/budget; the two commands are a flag set
    #                       each. -466, and the FIRST paydown.
    #   46,797 / 235 files  `llz extension list`. +81, spent deliberately: the
    #                       declaration model was validated with no way to look at
    #                       it, and a ratchet that could only be paid into would be
    #                       a freeze rather than a budget.
    #   46,106 / 235 files  guard-docs, the second extension. docs-guard and the
    #                       TOC renderer are tools/internal/docsguard; the two
    #                       commands are a flag set and a file write. -691, the
    #                       largest single move so far.
    #   45,763 / 234 files  posture-at-rest, the first extension that is not a
    #                       gate, plus the guardkit its extraction forced out.
    #                       -343.
    #   45,229 / 232 files  assert-storage, the first extension that touches a
    #                       cluster and a cloud — and the first whose extraction
    #                       needed seams injected rather than none at all. -534.
    #   44,826 / 228 files  reconcile-actions, four of the reconciler's eight
    #                       lanes. The four that stayed are the finding; see the
    #                       declaration. -403.
    #   44,171 / 226 files  teardown, the first TRANSITION binding. -655.
    #   43,817 / 221 files  template-sustain (partial — the own-paths half cannot
    #                       leave core) plus internal/color, which every extracted
    #                       extension needs. -354.
    #   40,827 / 214 files  import-brownfield, the first OPT-IN extension and the
    #                       largest single move: -2,990. Below the 41,803 this
    #                       gate first recorded, and below the pre-rebase number.
    #   38,821 / 206 files  obj-encryption, the first extension to bind `seeded` —
    #                       the group PR #15's ceiling banned by omission. -2,006,
    #                       plus internal/harborauth and internal/s3sig.
    #   38,364 / 202 files  guard-charts, the fourth gate — plus internal/guardwalk,
    #                       the manifest traversal TEN guards share. -457.
    #   37,483 / 199 files  cluster-access, the first extension whose binding is a
    #                       TRANSITION at `provisioned` — and the second to widen
    #                       grantStates (secret-custody, for the cloud-issued
    #                       bootstrap kubeconfig). Plus internal/tfbin. -881.
    #   37,131 / 197 files  health-sla, two NAMED invariants split on capability.
    #                       Only -352: internal/kubectlprobe (the 9th shared
    #                       package, 10 callers) left, but four cobra commands came
    #                       BACK as flag sets. A paydown is net of what returns.
    #   36,107 / 192 files  token-inventory, the FIRST binding at `configured` —
    #                       and the first extraction to add a WORD to the model
    #                       rather than a table row (secret-read, split out of
    #                       secret-custody). -1,024.
    #   34,359 / 188 files  converge, THE ACID TEST — the transition/assertion split
    #                       the catalog predicted, plus internal/cigate (12 callers).
    #                       -1,748, the second-largest single move.
    #   33,877 / 185 files  assert-platform, the first PURELY-assertion extension —
    #                       four lanes, one read grant each, nothing that mutates.
    #                       Only -482: the fifth file the catalog named was template
    #                       -pin machinery and stayed. 
    #   33,157 / 185 files  assert-reconciler, the SECOND opt-in extension and the
    #                       first half of a capability/assertion pair. Plus
    #                       internal/promwire. -720, with no file-count change: the
    #                       moved lines came back as Deps wiring and shared packages.
    #   32,965 / 185 files  assert-registry, the CHEAPEST extraction (closure 2) and
    #                       the only one needing NO injected capabilities. -192, the
    #                       smallest paydown so far — the cobra Long text is most of
    #                       what came back.
    #   32,733 / 184 files  promote-pipeline, which binds `promoted` — the LAST
    #                       unclaimed state. All ten now carry a binding. -232; the
    #                       os.WriteFile stayed in cmd/llz so the read-repo grant
    #                       remains true (3rd case, same resolution as gen-toc).
    #   32,077 / 182 files  posture-credential-coverage, a GATE the catalog filed
    #                       under `invariant: operating` — it reaches no cluster at
    #                       all. 5th catalog correction, 1st about a STATE rather
    #                       than a file list. -656, at 90.5% coverage.
    #   31,372 / 182 files  config-readiness — the `configured` PREDICATE, which the
    #                       catalog called right. Plus internal/instancelayout, a hub
    #                       extracted to break a CYCLE rather than on caller count:
    #                       it took env-topology's closure 21 -> 6. -705.
    #   30,687 / 179 files  env-topology, plus internal/yamledit (3 callers). -685.
    #                       branchpolicy.go went BACK to cmd/llz: its GitHub PUT is
    #                       cloud-mutate at `configured`, which grantStates refuses,
    #                       and one shipping case is below the two-case bar.
    #   29,853 / 176 files  assert-network — BELOW 30,000, and the best ratio yet:
    #                       closure 6 across 1,267 lines. -834.
    #   29,450 / 174 files  wave-health, a GATE the catalog filed as an invariant —
    #                       2nd state-level correction. Closure 3, all noise. -403.
    #   29,230 / 172 files  tofu-driver — three verbs the catalog gave ONE row and
    #                       one grant; two of the three mutate nothing. -220.
    #   27,156 / 161 files  assert-observability, the LARGEST assert row and the
    #                       second-biggest single move: -2,074. Adding ci_readiness
    #                       to the file set LOWERED closure 20 -> 14.
    #   26,174 / 158 files  assert-secrets. -982. Grepped for hidden writes BEFORE
    #                       declaring and found one: the broad-PAT drill applies a
    #                       Job. Four extractions, four hidden mutations.
    #   25,274 / 157 files  assert-identity. -900. FIVE for five on hidden
    #                       mutations: the "smoke test" CREATES a Keycloak client
    #                       and a user. First extraction the LANGUAGE forced into
    #                       a shared package — Go will not let two packages define
    #                       methods on a type neither owns, so internal/keycloak
    #                       had to come out first.
    #   25,020 / 156 files  deliver-docs. -254, the SMALLEST paydown yet, and the
    #                       one that cost the most model. It added `write-repo` —
    #                       the second word this vocabulary has gained, after
    #                       three earlier extractions recorded the gap and refused
    #                       to invent it. gen-toc, guard-docs and promote-pipeline
    #                       could each keep the os.WriteFile in cmd/llz; this one
    #                       PRUNES a directory mid-walk, so there was no seam to
    #                       split on and the declaration was impossible rather
    #                       than incomplete.
    #   24,807 / 155 files  argocd-diagnostics. -213. The FIRST extension whose
    #                       binding KIND is wrong on purpose: a diagnostic that
    #                       always exits 0, declared `assertion` and marked
    #                       Incomplete, because a fifth kind needs two cases and
    #                       this is one. Needed NO Deps — kubectlprobe.Exec
    #                       already had the only seam. EffectiveKubeconfig moved
    #                       to internal/kubectlprobe: closure measures what moved
    #                       files REACH, not what reaches them, so the inbound
    #                       caller in cluster-bootstrap was invisible until the
    #                       build broke.
    #   24,203 / 154 files  posture-plaintext. -604, and the CLEANEST BOUNDARY of
    #                       the campaign: a real closure of ZERO. 984 lines plus
    #                       719 of test moved with no injected capability, no
    #                       stranded fixture and no symbol crossing either way.
    #                       closure.py first said 5 — all five words inside error
    #                       STRINGS, because it stripped backtick literals before
    #                       double-quoted ones and a backtick inside a "..." ate
    #                       an arbitrary span. Every closure measured before that
    #                       fix is an UPPER BOUND.
    #   23,898 / 153 files  chart-publish. -305, and the THIRD grantStates
    #                       widening: cloud-mutate at `configured`. env-topology
    #                       wrote that exact binding 10 extractions ago, was
    #                       refused, and moved branchpolicy.go BACK to main rather
    #                       than widen on one case. This is case two, so the row
    #                       moved and env-topology's third binding is now
    #                       expressible. 7th catalog correction, and the SECOND
    #                       file pulled out of the `release-publish` row.
    #   23,653 / 150 files  guard-manifests, three lanes of four. -245. The
    #                       declaration's own test found the split: the first cut
    #                       called all three a gate, and TestPackageStaysFilesOnly
    #                       failed on `apl_schema.go reaches os/exec` — that lane
    #                       shells out to helm to resolve the chart it validates
    #                       against, so it is an assertion holding cloud-read.
    #                       8th catalog correction. dropped-apiversions stayed
    #                       (entangled with checks.go both ways), marked Incomplete.
    #   23,387 / 149 files  assert-objstore. -266. SIX FOR SIX on assert- lanes
    #                       that mutate — and the first where the write is NOT
    #                       hidden: its own header says it PUTs, GETs and deletes.
    #                       The write IS the check, because Linode's obj
    #                       generations are disjoint namespaces and the API's
    #                       bucket listing was the misleading view.
    #   23,205 / 148 files  wedge-gameday. -182. Cleanest boundary since
    #                       posture-plaintext: closure 2, one inbound edge, NO
    #                       Deps. SIXTH forced `transition:converged`, and the
    #                       first where BOTH halves are forced — a gameday belongs
    #                       at `operating` and Transition cannot reach it.
    #   22,964 / 146 files  phase-timing. -241, and a NEGATIVE RESULT worth more
    #                       than the lines: the SECOND diagnostic, so the two-case
    #                       bar for a fifth binding kind is met — and the two cases
    #                       DISAGREE about shape. Not one kind, so nothing invented.
    #   22,726 / 143 files  doctor-probes. -238. CASE THREE OF THREE closes the
    #                       fifth-kind question in the negative: the three
    #                       "diagnostics" attach to the FAILURE of a state, NO
    #                       state, and TWO states. "Diagnostic" is a tone of voice,
    #                       and the model already has a field for that (Short).
    #   22,566 / 142 files  kyverno-policies. -160. FIRST CATALOG NOTE THAT WAS
    #                       RIGHT FIRST TIME, after eight corrections. Also the
    #                       first UNFORCED transition:converged after six forced
    #                       ones — it genuinely acts. //go:embed PINS DATA to its
    #                       package dir, so the manifests could not follow.
    #   22,383 / 141 files  managed-fresh, into template-sustain's existing gate.
    #                       -183, and the NINTH catalog correction: the
    #                       `apl-upgrade` row groups two unrelated things, by WHEN
    #                       they run rather than what they touch. A coverage floor
    #                       went DOWN 84 -> 55 — the documented exception, because
    #                       the tests assert what the CLASS TABLE locks.
    #   22,153 / 140 files  dev-mutation-testing. -230, and the FIRST extension
    #                       that is not about the platform at all — its subject is
    #                       the repository's own tests. 97 lines of RunE moved with
    #                       it: a flag set that keeps its verb in a cobra callback
    #                       has been relabelled, not extracted. testdata/ moved
    #                       WITH the package, unlike kyverno's embedded manifests.
    #   21,841 / 138 files  release-publish. -312, TENTH catalog correction, and
    #                       the THIRD file out of this one row. Its note says
    #                       "template-repo-side, not instance-side" and three of
    #                       its five files are not. Also the FIRST row where moved
    #                       code got NO BINDING: publish-charts runs only in the
    #                       template repo's own workflow, so it has no instance
    #                       lifecycle state — a catalog row that is not an
    #                       extension.
    #   21,542 / 136 files  credential-state-passphrase. -299, and the FIRST
    #                       credential row extractable at all: the other five read
    #                       OpenBao through bao_read.go, whose verdict type has 7
    #                       callers and whose exec seam has 9. This one is not
    #                       blocked because the Tofu state passphrase never enters
    #                       OpenBao — it must exist before any cluster does. Its
    #                       binding is PUSHED to `seeded`: the honest state is
    #                       `configured`, and grantStates rightly refuses
    #                       secret-custody there.
    #   21,457 / 136 files  internal/baoread — INFRASTRUCTURE, not an extension.
    #                       Only -85, and that is the point: the OpenBao read
    #                       classifier is 170 lines that SEVEN files depend on, so
    #                       extracting it buys almost no surface and unblocks a
    #                       family. It did not unblock as much as predicted: the
    #                       credential rows share a SECOND framework
    #                       (credentials.go's rotator client + the rotation
    #                       table), which is the actual next wall.
    #   21,082 / 130 files  credential-pat + credential-objkey, on the
    #                       internal/credrotate framework. -375, and the SECOND
    #                       WALL down. The measurement proves it: credentials_pat
    #                       was unmeasurable in place and is closure TWO once the
    #                       framework moved. FIRST PACKAGE TO DECLARE TWO
    #                       EXTENSIONS — they share a framework, not a lifecycle.
    #                       A THIRD wall remains: the rotation table in
    #                       ci_rotate_linode_creds.go, 18 symbols with callers in
    #                       eight other files.
    #   20,835 / 127 files  the rotation table — WALL 3, and the last one. -247.
    #                       ci_rotate_linode_creds.go knows what a credential IS
    #                       in this platform (which exist, where each lives in
    #                       OpenBao, which Linode API mints it), so nothing that
    #                       handles one could leave while it stayed. Measured
    #                       result: every remaining file in the family dropped
    #                       from unmeasurable to closure 4-10.
    #   20,591 / 125 files  broad-PAT rotation + temp-objkey, into credrotate.
    #                       -244, and a FOURTH wall found: the other three files
    #                       (seed-broad-pat, mint-objkeys, incluster-pat) reach
    #                       the OpenBao WRITE/seed layer (baoKVPutFn, runCIBaoSeed,
    #                       baoExecFn) and the GitHub OIDC layer. My "these are
    #                       ordinary extractions now" was right for two of five —
    #                       per-file closures were clean, the COMBINED set was 16.
    #   20,492 / 122 files  wall 4, HALF DOWN. -99. The GitHub OIDC layer went to
    #                       internal/forge and two kubectl Secret probes that were
    #                       never the seeder's went to internal/kube (four callers,
    #                       none of which seeds anything). The set fell 16 -> 12.
    #                       What is left is the OpenBao WRITE path proper:
    #                       baoExecFn, baoKVPutFn, baoSeedOpts/runCIBaoSeed.
    #   20,269 / 120 files  wall 4 done for the credential family. -223. The
    #                       OpenBao WRITE seams (ExecStdin, KVPut) went into
    #                       internal/baoread rather than a new package — the read
    #                       classifier and the write use the SAME in-pod bao CLI,
    #                       and two packages installing one exec twice is worse
    #                       than a package whose name describes its better half.
    #                       mint-objkeys and incluster-pat followed.
    #   19,436 / 144 files  database-provisioner + assert-database. -833, the
    #                       largest single move since assert-observability. The
    #                       catalog's textbook capability/assertion pair, and the
    #                       code agreed — unlike credential-pat/objkey one
    #                       extraction earlier, which share a framework and NOT a
    #                       subject. Also the first binding refused by BOTH
    #                       tables: rotation belongs at `operating`, and the two
    #                       tables disagree about where it may go instead.
    #   19,074 / 142 files  openbao-seed — the seeding THIRD of the
    #                       openbao-lifecycle row. -362. The whole row measures 20
    #                       outbound against ~25 inbound across six other files:
    #                       not one boundary, six. The seeders ARE one, and only
    #                       because four earlier walls had already turned
    #                       baoKVPutFn into baoread.KVPut and openbaoNS into
    #                       baoread.Namespace.
    #   19,006 / 141 files  openbao-peer-ca — the CA slice of the same row, and
    #                       the SECOND slice taken. -68, the smallest paydown
    #                       since posture-plaintext's measurement fix, and the
    #                       point is the boundary rather than the lines: two verbs
    #                       whose entire inbound surface is their own cobra
    #                       constructors. secret_apply.go went to internal/kube on
    #                       the way — helpers that had already been orphaned TWICE
    #                       by the code they were typed next to.
    #
    # THE FILE COUNTS ABOVE ARE UNRELIABLE FROM ROUGHLY THE 30th ENTRY ON, and the
    # line counts are not. `exact: true` checks the NUMBER every commit, so every
    # budget figure here has been verified by the gate; nothing checks the "/ N
    # files" annotation, and it was being decremented by hand rather than measured.
    # By the database entry it read 115 against an actual 144. Corrected there;
    # earlier entries in that stretch are approximate and were left rather than
    # invented. Read `llz ci core-surface --verbose | wc -l` for the real count.

    # `exact: true` is what made this commit possible to get wrong and therefore
    # possible to get right: extracting the code and forgetting this line fails the
    # gate with "SHRANK — LOWER IT" and the new number, so the paydown is banked
    # rather than left as slack the next change grows into unremarked.
    budget: 19006
    exact: true
    include:
      - "tools/cmd/llz/*.go"

exclude:
  - "tools/cmd/llz/*_test.go"

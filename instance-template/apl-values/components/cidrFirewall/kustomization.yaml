# spec.components.cidrFirewall → this kustomize Component. An env's overlay
# lists it under components: when enabled (default-disabled — the controller
# itself is the Akamai-internal llz-linode-cidr-firewall chart added per
# docs/consume-lke-landing-zone-internal.md; this component ships the public
# support glue that feeds it). Template-managed — edit upstream.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - llz-cidr-firewall
